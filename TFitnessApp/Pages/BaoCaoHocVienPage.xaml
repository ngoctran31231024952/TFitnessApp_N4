<Page x:Class="TFitnessApp.BaoCaoHocVienPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      xmlns:local="clr-namespace:TFitnessApp"
      mc:Ignorable="d"
      d:DesignHeight="900"
      d:DesignWidth="1200"
      Background="#F0F2F5">

    <Page.DataContext>
        <local:DashboardViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="ScrollThe" TargetType="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <local:ChartCard Width="400"
                                 Height="400"
                                 TieuDe=" "
                                 HienChonLoaiBD="False" Loaded="ChartCard_Loaded_2">
                    <StackPanel VerticalAlignment="Center" Margin="10">
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DoanhSoGiaHanCuFormatted}"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#2ecc71"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Dang hoat dong"
                                           FontSize="13"
                                           Foreground="#666"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <Rectangle Grid.Column="1"
                                       Width="1"
                                       Height="40"
                                       Fill="#E0E0E0"
                                       Margin="15,0,15,0"
                                       VerticalAlignment="Center"/>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DoanhSoMoiFormatted}"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#D08700"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Gia han"
                                           FontSize="13"
                                           Foreground="#666"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DoanhSoGiaHanCuFormatted}"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#3498db"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Het hạn"
                                           FontSize="13"
                                           Foreground="#666"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <Rectangle Grid.Column="1"
                                       Width="1"
                                       Height="40"
                                       Fill="#E0E0E0"
                                       Margin="15,0,15,0"
                                       VerticalAlignment="Center"/>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DoanhSoMoiFormatted}"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#e74c3c"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Hoc vien mới"
                                           FontSize="13"
                                           Foreground="#666"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,20,0,0" HorizontalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </StackPanel>
                </local:ChartCard>

                <local:ChartCard Width="600"
                                 Height="400"
                                 Margin="5"
                                 TieuDe="Xếp hạng chi nhánh theo số lượng học viên"
                                 HienNutIn="False"
                                 HienChonLoaiBD="False"
                                 HienBoLocNgay="False"
                                 HienChonChiNhanh="False">
                    <lvc:CartesianChart Series="{Binding DSHocVienCN}" LegendLocation="None">
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Labels="{Binding ...}"/>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis LabelFormatter="{Binding ...}"/>
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </local:ChartCard>
            </WrapPanel>

            <ScrollViewer Style="{StaticResource ScrollThe}">
                <local:ChartCard Width="1050"
                                 Height="400"
                                 Margin="5"
                                 TieuDe="Biểu đồ tăng trưởng học viên"
                                 HienNutIn="False"
                                 HienChonLoaiBD="TRue"
                                 HienBoLocNgay="False"
                                 HienChonChiNhanh="False"
                                 Loaded="ChartCard_Loaded_1">
                    <lvc:CartesianChart Series="{Binding DSHVTangTruong}" LegendLocation="None">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding NhanNgayThang}"
                                      Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis LabelFormatter="{Binding FormatterTien}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </local:ChartCard>
            </ScrollViewer>

            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Có thể thêm các control khác tại đây -->
            </WrapPanel>
        </StackPanel>
    </ScrollViewer>
</Page>