<Window x:Class="TFitnessApp.Windows.ThemGoiTapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Thêm gói tập" Height="500" Width="700"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" FontFamily="Lato">

    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#A01516"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#C71A1B" CornerRadius="10,10,0,0">
                <Grid>
                    <TextBlock x:Name="txtHeaderTitle" Text="THÊM GÓI TẬP MỚI" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" Style="{StaticResource CloseButtonStyle}" Click="BtnHuy_Click">
                        <fa:IconImage Icon="Times" Width="14" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Form -->
            <Grid Grid.Row="1" Margin="104,30,94,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="#333"/>
                        <Setter Property="Margin" Value="0,0,0,20"/>
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Padding" Value="0,5"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#999"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Margin" Value="0,0,0,20"/>
                    </Style>
                </Grid.Resources>

                <TextBlock Grid.Row="0" Text="Mã gói"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtMaGoi" />

                <TextBlock Grid.Row="1" Text="Tên gói"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtTenGoi" />

                <TextBlock Grid.Row="2" Text="Thời hạn (tháng)"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtThoiHan" />

                <TextBlock Grid.Row="3" Text="Giá"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtGia" />

                <TextBlock Grid.Row="4" Text="Số buổi PT"/>
                <TextBox Grid.Row="4" Grid.Column="1" x:Name="txtSoBuoiPT" />

                <TextBlock Grid.Row="5" Text="Dịch vụ đặc biệt"/>
                <ComboBox Grid.Row="5" Grid.Column="1" x:Name="cmbDichVu" Margin="0,0,0,20" Padding="5">
                    <ComboBoxItem Content="Có"/>
                    <ComboBoxItem Content="Không" IsSelected="True"/>
                </ComboBox>

                <TextBlock Grid.Row="6" Text="Trạng thái"/>
                <ComboBox Grid.Row="6" Grid.Column="1" x:Name="cmbTrangThai" Padding="5">
                    <ComboBoxItem Content="Hoạt Động" IsSelected="True"/>
                    <ComboBoxItem Content="Ngừng Bán"/>
                </ComboBox>
            </Grid>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top">
                <Button Click="BtnLuu_Click" Background="#C71A1B" Foreground="White" FontWeight="Bold" FontSize="15" BorderThickness="0" Width="140" Height="45" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="22">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal">
                        <fa:IconImage Icon="Plus" Width="14" Foreground="White" Margin="0,0,8,0"/>
                        <TextBlock x:Name="txtBtnAction" Text="Tạo" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>