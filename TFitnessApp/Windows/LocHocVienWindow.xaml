<Window x:Class="TFitnessApp.Windows.LocHocVienWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Lọc danh sách học viên" Height="550" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" 
        FontFamily="Lato"
        Language="vi-VN">

    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#A01516"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#C71A1B" CornerRadius="10,10,0,0">
                <Grid>
                    <TextBlock Text="LỌC DANH SÁCH HỌC VIÊN" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0"/>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" Style="{StaticResource CloseButtonStyle}" Click="BtnHuy_Click">
                        <fa:IconImage Icon="Times" Width="14" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Form -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="30,10">

                    <TextBlock Text="Giới tính"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="rbNam" Content="Nam" Margin="0,0,20,0"/>
                        <RadioButton x:Name="rbNu" Content="Nữ" Margin="0,0,20,0"/>
                        <RadioButton x:Name="rbTatCa" Content="Tất cả" IsChecked="True"/>
                    </StackPanel>

                    <TextBlock Text="Năm sinh (Từ - Đến)"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <DatePicker x:Name="dpNamSinhTu" Padding="2"/>
                        <TextBlock Grid.Column="1" Text="-" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0"/>
                        <DatePicker Grid.Column="2" x:Name="dpNamSinhDen" Padding="2"/>
                    </Grid>

                    <TextBlock Text="Ngày tham gia (Từ - Đến)"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <DatePicker x:Name="dpThamGiaTu" Padding="2"/>
                        <TextBlock Grid.Column="1" Text="-" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0"/>
                        <DatePicker Grid.Column="2" x:Name="dpThamGiaDen" Padding="2"/>
                    </Grid>

                    <TextBlock Text="Gói tập"/>
                    <ComboBox x:Name="cmbGoiTap" DisplayMemberPath="Name" SelectedValuePath="ID" Padding="5"/>

                    <TextBlock Text="Chi nhánh"/>
                    <ComboBox x:Name="cmbChiNhanh" DisplayMemberPath="Name" SelectedValuePath="ID" Padding="5"/>

                    <TextBlock Text="PT Phụ trách"/>
                    <ComboBox x:Name="cmbPT" IsEditable="True" StaysOpenOnEdit="True"
                              DisplayMemberPath="Name" SelectedValuePath="ID"
                              Padding="5" KeyUp="cmbPT_KeyUp"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
                <Button Click="BtnApDung_Click" Background="#C71A1B" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Width="120" Height="40" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="Áp dụng" Foreground="White"/>
                </Button>

                <Button Click="BtnDatLai_Click" Background="White" BorderBrush="#CCC" BorderThickness="1" Foreground="#333" FontSize="14" Width="100" Height="40" Margin="15,0,0,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="Đặt lại"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>